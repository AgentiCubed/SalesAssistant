name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  repo-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify repo basics
        run: |
          test -f README.md
          test -f .gitignore || true
